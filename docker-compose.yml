version: "3.2"

services:
  delta-zk:
    build: .
    container_name: delta-zk
    image: "deltampc/delta-zk:dev"
    ports:
      - "4500:4500"
    volumes:
      - ./ptau:/app/ptau
      - ./circuits/main:/app/circuits/main
    command: ["yarn", "server"]

      